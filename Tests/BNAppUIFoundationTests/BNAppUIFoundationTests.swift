import XCTest
@testable import BNAppUIFoundation

final class BNAppUIFoundationTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(BNAppUIFoundation().text, "Hello, World!")
    }
}
